import React from "react";
import NotFound from "../not-found";
export const metadata = {
  title: "Page 404 | Ruper",
};

const NotFoundPage = () => {
  return <NotFound />;
};

export default NotFoundPage;
